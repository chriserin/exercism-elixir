[33mwarning: [0mvariable files is unused
  grep.exs:4

[33mwarning: [0mvariable flags is unused
  grep.exs:4

[33mwarning: [0mvariable pattern is unused
  grep.exs:4

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (3.5ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt"]) == "Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0mnil
     [36mrhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag  * test grepping a single file one match, print line numbers flag (skipped)
  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 20 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable files is unused
  grep.exs:4

[33mwarning: [0mvariable flags is unused
  grep.exs:4

[33mwarning: [0mvariable pattern is unused
  grep.exs:4

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (4.2ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt"]) == "Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0mnil
     [36mrhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag  * test grepping a single file one match, print line numbers flag (skipped)
  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 20 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable flags is unused
  grep.exs:4

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (3.9ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt"]) == "Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m["Of Atreus, Agamemnon, King of men."]
     [36mrhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag  * test grepping a single file one match, print line numbers flag (skipped)
  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 20 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable flags is unused
  grep.exs:4

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.5ms)[0m
  * test grepping a single file one match, print line numbers flag  * test grepping a single file one match, print line numbers flag (skipped)
  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.2s on load, 0.00s on tests)
[32m21 tests, 0 failures, 20 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable flags is unused
  grep.exs:4

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.4ms)[0m
  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (2.8ms)[0m

  1) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Forbidden", ["-n"], ["paradise-lost.txt"]) == "2:Of that Forbidden Tree, whose mortal tast\n"
     [36mlhs:  [0m"Of that Forbidden Tree, whose mortal tast\n"
     [36mrhs:  [0m"[32m2:[0mOf that Forbidden Tree, whose mortal tast\n"
     [36mstacktrace:[0m
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 19 skipped[0m

Randomized with seed 0
** (ArgumentError) invalid args for operator "in", it expects a compile-time list or compile-time range on the right side when used in guard expressions, got: flags
    (elixir) expanding macro: Kernel.in/2
    grep.exs:22: Grep.add_line_numbers/2
    (elixir) lib/code.ex:321: Code.load_file/2

** (ArgumentError) invalid args for operator "in", it expects a compile-time list or compile-time range on the right side when used in guard expressions, got: flags
    (elixir) expanding macro: Kernel.in/2
    grep.exs:22: Grep.add_line_numbers/2
    (elixir) lib/code.ex:321: Code.load_file/2

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (4.1ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31m** (FunctionClauseError) no function clause matching in String.split/3[0m
     [36mstacktrace:[0m
       (elixir) lib/string.ex:333: String.split({:ok, "Achilles sing, O Goddess! Peleus' son;\nHis wrath pernicious, who ten thousand woes\nCaused to Achaia's host, sent many a soul\nIllustrious into Ades premature,\nAnd Heroes gave (so stood the will of Jove)\nTo dogs and to all ravening fowls a prey,\nWhen fierce dispute had separated once\nThe noble Chief Achilles from the son\nOf Atreus, Agamemnon, King of men.\n"}, ~r/\R/, [])
       grep.exs:18: Grep.read_file/2
       (elixir) lib/enum.ex:1215: anonymous fn/4 in Enum.map_join/3
       (elixir) lib/enum.ex:1623: Enum."-reduce/3-lists^foldl/2-0-"/3
       (elixir) lib/enum.ex:1214: Enum.map_join/3
       grep.exs:7: Grep.grep/3
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (2.4ms)[0m

  2) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31m** (FunctionClauseError) no function clause matching in String.split/3[0m
     [36mstacktrace:[0m
       (elixir) lib/string.ex:333: String.split({:ok, "Of Mans First Disobedience, and the Fruit\nOf that Forbidden Tree, whose mortal tast\nBrought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nOf Oreb, or of Sinai, didst inspire\nThat Shepherd, who first taught the chosen Seed\n"}, ~r/\R/, [])
       grep.exs:18: Grep.read_file/2
       (elixir) lib/enum.ex:1215: anonymous fn/4 in Enum.map_join/3
       (elixir) lib/enum.ex:1623: Enum."-reduce/3-lists^foldl/2-0-"/3
       (elixir) lib/enum.ex:1214: Enum.map_join/3
       grep.exs:7: Grep.grep/3
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 2 failures, 19 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (4.6ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt"]) == "Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"Achilles sing, O Goddess! Peleus' son;His wrath pernicious, who ten thousand woesCaused to Achaia's host, sent many a soulIllustrious into Ades premature,And Heroes gave (so stood the will of Jove)To dogs and to all ravening fowls a prey,When fierce dispute had separated onceThe noble Chief Achilles from the sonOf Atreus, Agamemnon, King of men.\n"
     [36mrhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (3.5ms)[0m

  2) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31m** (BadArityError) #Function<0.132358712/2 in Grep.add_line_numbers/2> with arity 2 called with 1 argument ({"Of Mans First Disobedience, and the Fruit", 0})[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1184: Enum."-map/2-lists^map/1-0-"/2
       (elixir) lib/enum.ex:1215: anonymous fn/4 in Enum.map_join/3
       (elixir) lib/enum.ex:1623: Enum."-reduce/3-lists^foldl/2-0-"/3
       (elixir) lib/enum.ex:1214: Enum.map_join/3
       grep.exs:7: Grep.grep/3
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[31m21 tests, 2 failures, 19 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.7ms)[0m
  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (3.1ms)[0m

  1) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31m** (BadArityError) #Function<0.90916441/2 in Grep.add_line_numbers/2> with arity 2 called with 1 argument ({"Of Mans First Disobedience, and the Fruit", 0})[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1184: Enum."-map/2-lists^map/1-0-"/2
       grep.exs:27: Grep.add_line_numbers/2
       (elixir) lib/enum.ex:1215: anonymous fn/4 in Enum.map_join/3
       (elixir) lib/enum.ex:1623: Enum."-reduce/3-lists^foldl/2-0-"/3
       (elixir) lib/enum.ex:1214: Enum.map_join/3
       grep.exs:7: Grep.grep/3
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 19 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.9ms)[0m
  * test grepping a single file one match, case-insensitive flag  * test grepping a single file one match, case-insensitive flag (skipped)
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[32m21 tests, 0 failures, 19 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[31m  * test grepping a single file one match, case-insensitive flag (3.0ms)[0m

  1) test grepping a single file one match, case-insensitive flag (GrepTest)
     [1m[30mgrep_test.exs:74[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("FORBIDDEN", ["-i"], ["paradise-lost.txt"]) == "Of that Forbidden Tree, whose mortal tast\n"
     [36mlhs:  [0m""
     [36mrhs:  [0m"Of that Forbidden Tree, whose mortal tast\n"
     [36mstacktrace:[0m
       grep_test.exs:75: (test)

  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 18 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.9ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag  * test grepping a single file one match, print file names flag (skipped)
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[32m21 tests, 0 failures, 18 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (8.2ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31m** (Protocol.UndefinedError) protocol Enumerable not implemented for "Achilles sing, O Goddess! Peleus' son;His wrath pernicious, who ten thousand woesCaused to Achaia's host, sent many a soulIllustrious into Ades premature,And Heroes gave (so stood the will of Jove)To dogs and to all ravening fowls a prey,When fierce dispute had separated onceThe noble Chief Achilles from the sonOf Atreus, Agamemnon, King of men."[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1: Enumerable.impl_for!/1
       (elixir) lib/enum.ex:116: Enumerable.reduce/3
       (elixir) lib/enum.ex:1636: Enum.reduce/3
       (elixir) lib/enum.ex:791: Enum.filter/2
       grep.exs:9: Grep.grep/3
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (4.7ms)[0m

  2) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31m** (Protocol.UndefinedError) protocol Enumerable not implemented for "1:Of Mans First Disobedience, and the Fruit2:Of that Forbidden Tree, whose mortal tast3:Brought Death into the World, and all our woe,4:With loss of Eden, till one greater Man5:Restore us, and regain the blissful Seat,6:Sing Heav'nly Muse, that on the secret top7:Of Oreb, or of Sinai, didst inspire8:That Shepherd, who first taught the chosen Seed9:"[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1: Enumerable.impl_for!/1
       (elixir) lib/enum.ex:116: Enumerable.reduce/3
       (elixir) lib/enum.ex:1636: Enum.reduce/3
       (elixir) lib/enum.ex:791: Enum.filter/2
       grep.exs:9: Grep.grep/3
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag[31m  * test grepping a single file one match, case-insensitive flag (3.6ms)[0m

  3) test grepping a single file one match, case-insensitive flag (GrepTest)
     [1m[30mgrep_test.exs:74[0m
     [31m** (Protocol.UndefinedError) protocol Enumerable not implemented for "Of Mans First Disobedience, and the FruitOf that Forbidden Tree, whose mortal tastBrought Death into the World, and all our woe,With loss of Eden, till one greater ManRestore us, and regain the blissful Seat,Sing Heav'nly Muse, that on the secret topOf Oreb, or of Sinai, didst inspireThat Shepherd, who first taught the chosen Seed"[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1: Enumerable.impl_for!/1
       (elixir) lib/enum.ex:116: Enumerable.reduce/3
       (elixir) lib/enum.ex:1636: Enum.reduce/3
       (elixir) lib/enum.ex:791: Enum.filter/2
       grep.exs:9: Grep.grep/3
       grep_test.exs:75: (test)

  * test grepping a single file one match, print file names flag[31m  * test grepping a single file one match, print file names flag (4.0ms)[0m

  4) test grepping a single file one match, print file names flag (GrepTest)
     [1m[30mgrep_test.exs:81[0m
     [31m** (Protocol.UndefinedError) protocol Enumerable not implemented for "Of Mans First Disobedience, and the FruitOf that Forbidden Tree, whose mortal tastBrought Death into the World, and all our woe,With loss of Eden, till one greater ManRestore us, and regain the blissful Seat,Sing Heav'nly Muse, that on the secret topOf Oreb, or of Sinai, didst inspireThat Shepherd, who first taught the chosen Seed"[0m
     [36mstacktrace:[0m
       (elixir) lib/enum.ex:1: Enumerable.impl_for!/1
       (elixir) lib/enum.ex:116: Enumerable.reduce/3
       (elixir) lib/enum.ex:1636: Enum.reduce/3
       (elixir) lib/enum.ex:791: Enum.filter/2
       grep.exs:9: Grep.grep/3
       grep_test.exs:82: (test)

  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 4 failures, 17 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (6.6ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31m** (UndefinedFunctionError) function Enum.flatten/1 is undefined or private[0m
     [36mstacktrace:[0m
       (elixir) Enum.flatten([["Achilles sing, O Goddess! Peleus' son;", "His wrath pernicious, who ten thousand woes", "Caused to Achaia's host, sent many a soul", "Illustrious into Ades premature,", "And Heroes gave (so stood the will of Jove)", "To dogs and to all ravening fowls a prey,", "When fierce dispute had separated once", "The noble Chief Achilles from the son", "Of Atreus, Agamemnon, King of men.", ""]])
       grep.exs:9: Grep.grep/3
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (6.4ms)[0m

  2) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31m** (UndefinedFunctionError) function Enum.flatten/1 is undefined or private[0m
     [36mstacktrace:[0m
       (elixir) Enum.flatten([["1:Of Mans First Disobedience, and the Fruit", "2:Of that Forbidden Tree, whose mortal tast", "3:Brought Death into the World, and all our woe,", "4:With loss of Eden, till one greater Man", "5:Restore us, and regain the blissful Seat,", "6:Sing Heav'nly Muse, that on the secret top", "7:Of Oreb, or of Sinai, didst inspire", "8:That Shepherd, who first taught the chosen Seed", "9:"]])
       grep.exs:9: Grep.grep/3
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag[31m  * test grepping a single file one match, case-insensitive flag (3.6ms)[0m

  3) test grepping a single file one match, case-insensitive flag (GrepTest)
     [1m[30mgrep_test.exs:74[0m
     [31m** (UndefinedFunctionError) function Enum.flatten/1 is undefined or private[0m
     [36mstacktrace:[0m
       (elixir) Enum.flatten([["Of Mans First Disobedience, and the Fruit", "Of that Forbidden Tree, whose mortal tast", "Brought Death into the World, and all our woe,", "With loss of Eden, till one greater Man", "Restore us, and regain the blissful Seat,", "Sing Heav'nly Muse, that on the secret top", "Of Oreb, or of Sinai, didst inspire", "That Shepherd, who first taught the chosen Seed", ""]])
       grep.exs:9: Grep.grep/3
       grep_test.exs:75: (test)

  * test grepping a single file one match, print file names flag[31m  * test grepping a single file one match, print file names flag (1.7ms)[0m

  4) test grepping a single file one match, print file names flag (GrepTest)
     [1m[30mgrep_test.exs:81[0m
     [31m** (UndefinedFunctionError) function Enum.flatten/1 is undefined or private[0m
     [36mstacktrace:[0m
       (elixir) Enum.flatten([["Of Mans First Disobedience, and the Fruit", "Of that Forbidden Tree, whose mortal tast", "Brought Death into the World, and all our woe,", "With loss of Eden, till one greater Man", "Restore us, and regain the blissful Seat,", "Sing Heav'nly Muse, that on the secret top", "Of Oreb, or of Sinai, didst inspire", "That Shepherd, who first taught the chosen Seed", ""]])
       grep.exs:9: Grep.grep/3
       grep_test.exs:82: (test)

  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 4 failures, 17 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.9ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[31m  * test grepping a single file one match, print file names flag (3.1ms)[0m

  1) test grepping a single file one match, print file names flag (GrepTest)
     [1m[30mgrep_test.exs:81[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Forbidden", ["-l"], ["paradise-lost.txt"]) == "paradise-lost.txt\n"
     [36mlhs:  [0m"Of that Forbidden Tree, whose mortal tast\n"
     [36mrhs:  [0m"paradise-lost.txt\n"
     [36mstacktrace:[0m
       grep_test.exs:82: (test)

  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[31m21 tests, 1 failure, 17 skipped[0m

Randomized with seed 0
[33mwarning: [0mfunction add_line_numbers/3 is unused
  grep.exs:25

** (CompileError) grep.exs:22: undefined function add_line_numbers/4
    (stdlib) lists.erl:1338: :lists.foreach/2
    (stdlib) erl_eval.erl:670: :erl_eval.do_apply/6

[33mwarning: [0mfunction add_line_numbers/3 is unused
  grep.exs:25

** (CompileError) grep.exs:22: undefined function add_line_numbers/4
    (stdlib) lists.erl:1338: :lists.foreach/2
    (stdlib) erl_eval.erl:670: :erl_eval.do_apply/6

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (0.9ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag  * test grepping a single file one match, match entire lines flag (skipped)
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[32m21 tests, 0 failures, 17 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.8ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.2ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.0ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags  * test grepping a single file one match, multiple flags (skipped)
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.00s on tests)
[32m21 tests, 0 failures, 16 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.5ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.7ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.1ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (2.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (2.2ms)[0m
  * test grepping a single file several matches, no flags  * test grepping a single file several matches, no flags (skipped)
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 15 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (0.9ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag  * test grepping a single file several matches, print line numbers flag (skipped)
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 14 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.8ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.6ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.5ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.8ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.7ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (2.3ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.9ms)[0m
  * test grepping a single file several matches, match entire lines flag  * test grepping a single file several matches, match entire lines flag (skipped)
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 13 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.2ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.8ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.1ms)[0m
  * test grepping a single file several matches, match entire lines flag[31m  * test grepping a single file several matches, match entire lines flag (3.0ms)[0m

  1) test grepping a single file several matches, match entire lines flag (GrepTest)
     [1m[30mgrep_test.exs:122[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("may", ["-x"], ["midsummer-night.txt"]) == ""
     [36mlhs:  [0m"Nor how it may concern my modesty,\nBut I beseech your grace that I may know\nThe worst that may befall me in this case,\n"
     [36mrhs:  [0m""
     [36mstacktrace:[0m
       grep_test.exs:123: (test)

  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[31m21 tests, 1 failure, 12 skipped[0m

Randomized with seed 0
[33mwarning: [0mfunction match_entire_line/3 is unused
  grep.exs:47

** (CompileError) grep.exs:13: undefined function match_entire_line/2
    (stdlib) lists.erl:1338: :lists.foreach/2
    (stdlib) erl_eval.erl:670: :erl_eval.do_apply/6

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.0ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.4ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.5ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.5ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.6ms)[0m
  * test grepping a single file one match, multiple flags[31m  * test grepping a single file one match, multiple flags (5.9ms)[0m

  1) test grepping a single file one match, multiple flags (GrepTest)
     [1m[30mgrep_test.exs:96[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("OF ATREUS, Agamemnon, KIng of MEN.", ["-n", "-i", "-x"], ["iliad.txt"]) == "9:Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m""
     [36mrhs:  [0m"9:Of Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:97: (test)

  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (3.6ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (5.0ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (2.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 12 skipped[0m

Randomized with seed 0
** (SyntaxError) grep.exs:51: unexpected token: "~" (column 32, codepoint U+007E)
    (elixir) lib/code.ex:321: Code.load_file/2
    grep_test.exs:2: (file)
    (elixir) lib/code.ex:363: Code.require_file/2

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.9ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (0.9ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (0.9ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.2ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag  * test grepping a single file several matches, case-insensitive flag (skipped)
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 12 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.1ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (0.9ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.8ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (0.9ms)[0m
  * test grepping a single file several matches, inverted flag  * test grepping a single file several matches, inverted flag (skipped)
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 11 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.9ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (0.9ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.8ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.1ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (0.8ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.0ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag[31m  * test grepping a single file several matches, inverted flag (2.9ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"[31mOf[0m [31mM[0ma[31mns[0m [31mF[0mi[31mrs[0mt [31mD[0miso[31mbe[0mdien[31mc[0me, and the [31mFru[0mit\n[31mOf[0m that [31mF[0mo[31mrbidde[0mn [31mTr[0me[31me,[0m [31mwho[0mse[31m mo[0mrt[31mal[0m t[31mast[0m\n[31mOf[0m [31mOr[0me[31mb[0m, o[31mr[0m [31mo[0mf[31m S[0mi[31mn[0ma[31mi,[0m [31mdids[0mt [31min[0ms[31mpir[0me\n"
     [36mrhs:  [0m"[32mBrought[0m [32mDe[0ma[32mth[0m i[32mn[0mt[32mo[0m [32mthe World, and all our woe,\nW[0mi[32mth lo[0ms[32ms [0mo[32mf E[0md[32men, t[0mi[32mll on[0me[32m greater Ma[0mn[32m\nR[0me[32mstore us[0m, and [32mregain [0mthe [32mbl[0mi[32mssful Sea[0mt[32m,[0m\n[32mSing[0m [32mHeav'nly Muse, [0mthat on [32mth[0me se[32mc[0mr[32me[0mt t[32mop[0m\n[32mThat[0m [32mSh[0me[32mpherd[0m, [32mwh[0mo fi[32mrst t[0ma[32mught[0m t[32mhe[0m [32mcho[0mse[32mn Seed[0m\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
** (SyntaxError) grep.exs:53: syntax error before: 'not'
    (elixir) lib/code.ex:321: Code.load_file/2
    grep_test.exs:2: (file)
    (elixir) lib/code.ex:363: Code.require_file/2

** (CompileError) grep.exs:46: unhandled operator ->
    (elixir) expanding macro: Kernel.in/2
    grep.exs:45: Grep.match_line/3
    (elixir) expanding macro: Kernel.and/2
    grep.exs:45: Grep.match_line/3

** (CompileError) grep.exs:46: unhandled operator ->
    (elixir) expanding macro: Kernel.|>/2
    grep.exs:45: Grep.match_line/3
    (elixir) lib/code.ex:321: Code.load_file/2

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.8ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (2.5ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (5.4ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (7.5ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (5.8ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (8.5ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (6.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (5.8ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (7.6ms)[0m
  * test grepping a single file several matches, inverted flag[31m  * test grepping a single file several matches, inverted flag (9.6ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n[31m\n[0m"
     [36mrhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.1s on load, 0.07s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.3ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.6ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.1ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.1ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.3ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.4ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.5ms)[0m
  * test grepping a single file several matches, inverted flag[31m  * test grepping a single file several matches, inverted flag (3.5ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n[31m\n[0m"
     [36mrhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (1.5ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (0.9ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.1ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m
  * test grepping a single file several matches, inverted flag[31m  * test grepping a single file several matches, inverted flag (3.1ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n[31m\n[0m"
     [36mrhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[31m  * test grepping a single file one match, no flags (3.4ms)[0m

  1) test grepping a single file one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:60[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt"]) == "Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"Of Atreus, Agamemnon, King of men."
     [36mrhs:  [0m"Of Atreus, Agamemnon, King of men.[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:61: (test)

  * test grepping a single file one match, print line numbers flag[31m  * test grepping a single file one match, print line numbers flag (6.0ms)[0m

  2) test grepping a single file one match, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:67[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Forbidden", ["-n"], ["paradise-lost.txt"]) == "2:Of that Forbidden Tree, whose mortal tast\n"
     [36mlhs:  [0m"2:Of that Forbidden Tree, whose mortal tast"
     [36mrhs:  [0m"2:Of that Forbidden Tree, whose mortal tast[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:68: (test)

  * test grepping a single file one match, case-insensitive flag[31m  * test grepping a single file one match, case-insensitive flag (1.2ms)[0m

  3) test grepping a single file one match, case-insensitive flag (GrepTest)
     [1m[30mgrep_test.exs:74[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("FORBIDDEN", ["-i"], ["paradise-lost.txt"]) == "Of that Forbidden Tree, whose mortal tast\n"
     [36mlhs:  [0m"Of that Forbidden Tree, whose mortal tast"
     [36mrhs:  [0m"Of that Forbidden Tree, whose mortal tast[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:75: (test)

  * test grepping a single file one match, print file names flag[31m  * test grepping a single file one match, print file names flag (0.9ms)[0m

  4) test grepping a single file one match, print file names flag (GrepTest)
     [1m[30mgrep_test.exs:81[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Forbidden", ["-l"], ["paradise-lost.txt"]) == "paradise-lost.txt\n"
     [36mlhs:  [0m"paradise-lost.txt"
     [36mrhs:  [0m"paradise-lost.txt[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:82: (test)

  * test grepping a single file one match, match entire lines flag[31m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m

  5) test grepping a single file one match, match entire lines flag (GrepTest)
     [1m[30mgrep_test.exs:88[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("With loss of Eden, till one greater Man", ["-x"], ["paradise-lost.txt"]) == "With loss of Eden, till one greater Man\n"
     [36mlhs:  [0m"With loss of Eden, till one greater Man"
     [36mrhs:  [0m"With loss of Eden, till one greater Man[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:89: (test)

  * test grepping a single file one match, multiple flags[31m  * test grepping a single file one match, multiple flags (1.9ms)[0m

  6) test grepping a single file one match, multiple flags (GrepTest)
     [1m[30mgrep_test.exs:96[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("OF ATREUS, Agamemnon, KIng of MEN.", ["-n", "-i", "-x"], ["iliad.txt"]) == "9:Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"9:Of Atreus, Agamemnon, King of men."
     [36mrhs:  [0m"9:Of Atreus, Agamemnon, King of men.[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:97: (test)

  * test grepping a single file several matches, no flags[31m  * test grepping a single file several matches, no flags (1.4ms)[0m

  7) test grepping a single file several matches, no flags (GrepTest)
     [1m[30mgrep_test.exs:104[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("may", [], ["midsummer-night.txt"]) == "Nor how it may concern my modesty,\nBut I beseech your grace that I may know\nThe worst that may befall me in this case,\n"
     [36mlhs:  [0m"Nor how it may concern my modesty,\nBut I beseech your grace that I may know\nThe worst that may befall me in this case,"
     [36mrhs:  [0m"Nor how it may concern my modesty,\nBut I beseech your grace that I may know\nThe worst that may befall me in this case,[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:105: (test)

  * test grepping a single file several matches, print line numbers flag[31m  * test grepping a single file several matches, print line numbers flag (1.9ms)[0m

  8) test grepping a single file several matches, print line numbers flag (GrepTest)
     [1m[30mgrep_test.exs:113[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("may", ["-n"], ["midsummer-night.txt"]) == "3:Nor how it may concern my modesty,\n5:But I beseech your grace that I may know\n6:The worst that may befall me in this case,\n"
     [36mlhs:  [0m"3:Nor how it may concern my modesty,\n5:But I beseech your grace that I may know\n6:The worst that may befall me in this case,"
     [36mrhs:  [0m"3:Nor how it may concern my modesty,\n5:But I beseech your grace that I may know\n6:The worst that may befall me in this case,[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:114: (test)

  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (2.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[31m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m

  9) test grepping a single file several matches, case-insensitive flag (GrepTest)
     [1m[30mgrep_test.exs:126[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("ACHILLES", ["-i"], ["iliad.txt"]) == "Achilles sing, O Goddess! Peleus' son;\nThe noble Chief Achilles from the son\n"
     [36mlhs:  [0m"Achilles sing, O Goddess! Peleus' son;\nThe noble Chief Achilles from the son"
     [36mrhs:  [0m"Achilles sing, O Goddess! Peleus' son;\nThe noble Chief Achilles from the son[32m\n[0m"
     [36mstacktrace:[0m
       grep_test.exs:127: (test)

  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.7ms)[0m
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 9 failures, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.9ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.0ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m
  * test grepping a single file several matches, inverted flag[31m  * test grepping a single file several matches, inverted flag (3.2ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n[31m\n[0m"
     [36mrhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags["Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, no flags (3.4ms)[0m
  * test grepping a single file one match, print line numbers flag["2:Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag["Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag["paradise-lost.txt"]
[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag["With loss of Eden, till one greater Man"]
[32m  * test grepping a single file one match, match entire lines flag (1.2ms)[0m
  * test grepping a single file one match, multiple flags["9:Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, multiple flags (1.2ms)[0m
  * test grepping a single file several matches, no flags["Nor how it may concern my modesty,",
 "But I beseech your grace that I may know",
 "The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag["3:Nor how it may concern my modesty,",
 "5:But I beseech your grace that I may know",
 "6:The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, print line numbers flag (1.1ms)[0m
  * test grepping a single file several matches, match entire lines flag[]
[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag["Achilles sing, O Goddess! Peleus' son;",
 "The noble Chief Achilles from the son"]
[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag["Brought Death into the World, and all our woe,",
 "With loss of Eden, till one greater Man",
 "Restore us, and regain the blissful Seat,",
 "Sing Heav'nly Muse, that on the secret top",
 "That Shepherd, who first taught the chosen Seed", ""]
[31m  * test grepping a single file several matches, inverted flag (3.5ms)[0m

  1) test grepping a single file several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:134[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Of", ["-v"], ["paradise-lost.txt"]) == "Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mlhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n[31m\n[0m"
     [36mrhs:  [0m"Brought Death into the World, and all our woe,\nWith loss of Eden, till one greater Man\nRestore us, and regain the blissful Seat,\nSing Heav'nly Muse, that on the secret top\nThat Shepherd, who first taught the chosen Seed\n"
     [36mstacktrace:[0m
       grep_test.exs:135: (test)

  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags["Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, no flags (3.6ms)[0m
  * test grepping a single file one match, print line numbers flag["2:Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag["Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag["paradise-lost.txt"]
[32m  * test grepping a single file one match, print file names flag (1.1ms)[0m
  * test grepping a single file one match, match entire lines flag["With loss of Eden, till one greater Man"]
[32m  * test grepping a single file one match, match entire lines flag (1.7ms)[0m
  * test grepping a single file one match, multiple flags["9:Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, multiple flags (1.4ms)[0m
  * test grepping a single file several matches, no flags["Nor how it may concern my modesty,",
 "But I beseech your grace that I may know",
 "The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, no flags (1.8ms)[0m
  * test grepping a single file several matches, print line numbers flag["3:Nor how it may concern my modesty,",
 "5:But I beseech your grace that I may know",
 "6:The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, print line numbers flag (1.3ms)[0m
  * test grepping a single file several matches, match entire lines flag[]
[31m  * test grepping a single file several matches, match entire lines flag (4.7ms)[0m
["Achilles sing, O Goddess! Peleus' son;",
 "The noble Chief Achilles from the son"]

  1) test grepping a single file several matches, match entire lines flag (GrepTest)
     [1m[30mgrep_test.exs:122[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("may", ["-x"], ["midsummer-night.txt"]) == ""
     [36mlhs:  [0m"\n"
     [36mrhs:  [0m""
     [36mstacktrace:[0m
       grep_test.exs:123: (test)

  * test grepping a single file several matches, case-insensitive flag["Brought Death into the World, and all our woe,",
 "With loss of Eden, till one greater Man",
 "Restore us, and regain the blissful Seat,",
 "Sing Heav'nly Muse, that on the secret top",
 "That Shepherd, who first taught the chosen Seed", ""]
[32m  * test grepping a single file several matches, case-insensitive flag (5.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (3.2ms)[0m
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 10 skipped[0m

Randomized with seed 0
** (TokenMissingError) grep.exs:81: missing terminator: end (for "do" starting at line 1)
    (elixir) lib/code.ex:321: Code.load_file/2
    grep_test.exs:2: (file)
    (elixir) lib/code.ex:363: Code.require_file/2

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags["Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, no flags (3.4ms)[0m
  * test grepping a single file one match, print line numbers flag["2:Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag["Of that Forbidden Tree, whose mortal tast"]
[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag["paradise-lost.txt"]
[32m  * test grepping a single file one match, print file names flag (1.0ms)[0m
  * test grepping a single file one match, match entire lines flag["With loss of Eden, till one greater Man"]
[32m  * test grepping a single file one match, match entire lines flag (1.1ms)[0m
  * test grepping a single file one match, multiple flags["9:Of Atreus, Agamemnon, King of men."]
[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags["Nor how it may concern my modesty,",
 "But I beseech your grace that I may know",
 "The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag["3:Nor how it may concern my modesty,",
 "5:But I beseech your grace that I may know",
 "6:The worst that may befall me in this case,"]
[32m  * test grepping a single file several matches, print line numbers flag (1.3ms)[0m
  * test grepping a single file several matches, match entire lines flag[]
[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag["Achilles sing, O Goddess! Peleus' son;",
 "The noble Chief Achilles from the son"]
[32m  * test grepping a single file several matches, case-insensitive flag (1.2ms)[0m
  * test grepping a single file several matches, inverted flag["Brought Death into the World, and all our woe,",
 "With loss of Eden, till one greater Man",
 "Restore us, and regain the blissful Seat,",
 "Sing Heav'nly Muse, that on the secret top",
 "That Shepherd, who first taught the chosen Seed", ""]
[32m  * test grepping a single file several matches, inverted flag (1.1ms)[0m
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.5ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (0.9ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (0.9ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (0.9ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags  * test grepping a single file no matches, various flags (skipped)
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 10 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.3ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.0ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.1ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (0.9ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (0.9ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.1ms)[0m
  * test grepping multiples files at once one match, no flags  * test grepping multiples files at once one match, no flags (skipped)
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.01s on tests)
[32m21 tests, 0 failures, 9 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (5.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (2.4ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (2.5ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.3ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (2.6ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (2.3ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.3ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.2ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.1ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.1ms)[0m
  * test grepping multiples files at once one match, no flags[31m  * test grepping multiples files at once one match, no flags (3.5ms)[0m

  1) test grepping multiples files at once one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:152[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mrhs:  [0m"[32miliad.txt:[0mOf Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:153: (test)

  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 8 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.5ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.5ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.8ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (0.9ms)[0m
  * test grepping multiples files at once one match, no flags[31m  * test grepping multiples files at once one match, no flags (4.3ms)[0m

  1) test grepping multiples files at once one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:151[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mrhs:  [0m"[32miliad.txt:[0mOf Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:152: (test)

  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 1 failure, 8 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.8ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (0.9ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (5.5ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.1ms)[0m
  * test grepping multiples files at once one match, no flags[31m  * test grepping multiples files at once one match, no flags (3.1ms)[0m

  1) test grepping multiples files at once one match, no flags (GrepTest)
     [1m[30mgrep_test.exs:151[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("Agamemnon", [], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Of Atreus, Agamemnon, King of men.\n"
     [36mlhs:  [0m"Of Atreus, Agamemnon, King of men.\n"
     [36mrhs:  [0m"[32miliad.txt:[0mOf Atreus, Agamemnon, King of men.\n"
     [36mstacktrace:[0m
       grep_test.exs:152: (test)

  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 1 failure, 8 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.3ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.0ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.2ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.0ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (3.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (2.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.6ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.5ms)[0m
  * test grepping multiples files at once several matches, no flags  * test grepping multiples files at once several matches, no flags (skipped)
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[32m21 tests, 0 failures, 8 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.2ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.2ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.4ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.7ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.2ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.5ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.3ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.8ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (4.1ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag  * test grepping multiples files at once several matches, print line numbers flag (skipped)
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[32m21 tests, 0 failures, 7 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (4.2ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (2.0ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.9ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (2.7ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.4ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.7ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (0.9ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.4ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.6ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.4ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (0.9ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.0ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.4ms)[0m
  * test grepping multiples files at once one match, print file names flag  * test grepping multiples files at once one match, print file names flag (skipped)
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.1s on load, 0.03s on tests)
[32m21 tests, 0 failures, 6 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.3ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.8ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (0.9ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.0ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.5ms)[0m
  * test grepping multiples files at once one match, print file names flag[31m  * test grepping multiples files at once one match, print file names flag (3.0ms)[0m

  1) test grepping multiples files at once one match, print file names flag (GrepTest)
     [1m[30mgrep_test.exs:180[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("who", ["-l"], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt\nparadise-lost.txt\n"
     [36mlhs:  [0m"iliad.txt[31m:iliad.txt[0m\nparadise-lost.txt[31m:paradise-lost.txt[0m\n"
     [36mrhs:  [0m"iliad.txt\nparadise-lost.txt\n"
     [36mstacktrace:[0m
       grep_test.exs:181: (test)

  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.02s on tests)
[31m21 tests, 1 failure, 5 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.5ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (2.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.6ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.4ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (2.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (2.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (2.4ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.4ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (3.3ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.2ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.1ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.1ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.5ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.8ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.5ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (1.8ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag  * test grepping multiples files at once several matches, case-insensitive flag (skipped)
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.04s on tests)
[32m21 tests, 0 failures, 5 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.2ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.6ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.5ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.9ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.3ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (2.5ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.9ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (2.0ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (2.2ms)[0m
  * test grepping multiples files at once several matches, inverted flag  * test grepping multiples files at once several matches, inverted flag (skipped)
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.03s on tests)
[32m21 tests, 0 failures, 4 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.2ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.8ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.0ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (0.9ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (0.9ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (0.9ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.3ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.7ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (1.6ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (1.7ms)[0m
  * test grepping multiples files at once several matches, inverted flag[31m  * test grepping multiples files at once several matches, inverted flag (6.0ms)[0m

  1) test grepping multiples files at once several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:206[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("a", ["-v"], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mlhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\n[31miliad.txt:\n[0mmidsummer-night.txt:If I refuse to wed Demetrius.\n[31mmidsummer-night.txt:\nparadise-lost.txt:\n[0m"
     [36mrhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mstacktrace:[0m
       grep_test.exs:207: (test)

  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 3 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.5ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.4ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.4ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.4ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (3.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.3ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (2.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.7ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (2.2ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.1ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.2ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.3ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.5ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.3ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.6ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (1.3ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (2.2ms)[0m
  * test grepping multiples files at once several matches, inverted flag[31m  * test grepping multiples files at once several matches, inverted flag (3.4ms)[0m

  1) test grepping multiples files at once several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:206[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("a", ["-v"], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mlhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\n[31miliad.txt:\n[0mmidsummer-night.txt:If I refuse to wed Demetrius.\n[31mmidsummer-night.txt:\nparadise-lost.txt:\n[0m"
     [36mrhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mstacktrace:[0m
       grep_test.exs:207: (test)

  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.04s on tests)
[31m21 tests, 1 failure, 3 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.6ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (0.9ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (0.9ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.6ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.0ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.0ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.3ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.1ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (1.2ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (1.1ms)[0m
  * test grepping multiples files at once several matches, inverted flag[31m  * test grepping multiples files at once several matches, inverted flag (3.5ms)[0m

  1) test grepping multiples files at once several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:206[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("a", ["-v"], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mlhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\n[31miliad.txt:\n[0mmidsummer-night.txt:If I refuse to wed Demetrius.\n[31mmidsummer-night.txt:\nparadise-lost.txt:\n[0m"
     [36mrhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mstacktrace:[0m
       grep_test.exs:207: (test)

  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 3 skipped[0m

Randomized with seed 0
Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.2ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (0.9ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.0ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.0ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.0ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (0.9ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.2ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.0ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.2ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.4ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (1.2ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (1.1ms)[0m
  * test grepping multiples files at once several matches, inverted flag[31m  * test grepping multiples files at once several matches, inverted flag (3.4ms)[0m

  1) test grepping multiples files at once several matches, inverted flag (GrepTest)
     [1m[30mgrep_test.exs:206[0m
     [31mAssertion with == failed[0m
     [36mcode: [0mGrep.grep("a", ["-v"], ["iliad.txt", "midsummer-night.txt", "paradise-lost.txt"]) == "iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mlhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\n[31miliad.txt:\n[0mmidsummer-night.txt:If I refuse to wed Demetrius.\n[31mmidsummer-night.txt:\nparadise-lost.txt:\n[0m"
     [36mrhs:  [0m"iliad.txt:Achilles sing, O Goddess! Peleus' son;\niliad.txt:The noble Chief Achilles from the son\nmidsummer-night.txt:If I refuse to wed Demetrius.\n"
     [36mstacktrace:[0m
       grep_test.exs:207: (test)

  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.1 seconds (0.1s on load, 0.03s on tests)
[31m21 tests, 1 failure, 3 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable file is unused
  grep.exs:16

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.3ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.0ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.2ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.2ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.0ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.3ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.1ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.2ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.3ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (3.0ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.5ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (2.1ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (2.4ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (2.9ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (2.0ms)[0m
  * test grepping multiples files at once several matches, inverted flag[32m  * test grepping multiples files at once several matches, inverted flag (2.4ms)[0m
  * test grepping multiples files at once one match, match entire lines flag  * test grepping multiples files at once one match, match entire lines flag (skipped)
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.1s on load, 0.04s on tests)
[32m21 tests, 0 failures, 3 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable file is unused
  grep.exs:16

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (2.4ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.1ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (1.4ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.4ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.6ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (2.1ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.3ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.7ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.5ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (2.4ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.2ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (2.1ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.9ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.6ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (1.6ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (2.0ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (1.9ms)[0m
  * test grepping multiples files at once several matches, inverted flag[32m  * test grepping multiples files at once several matches, inverted flag (5.8ms)[0m
  * test grepping multiples files at once one match, match entire lines flag[32m  * test grepping multiples files at once one match, match entire lines flag (1.1ms)[0m
  * test grepping multiples files at once one match, multiple flags  * test grepping multiples files at once one match, multiple flags (skipped)
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.2 seconds (0.2s on load, 0.04s on tests)
[32m21 tests, 0 failures, 2 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable file is unused
  grep.exs:16

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (13.7ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (3.5ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (5.1ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (1.6ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (1.7ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (1.2ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (1.5ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (5.6ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (0.9ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.0ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (0.9ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (2.9ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (9.0ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (14.6ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (8.9ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (3.7ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (2.5ms)[0m
  * test grepping multiples files at once several matches, inverted flag[32m  * test grepping multiples files at once several matches, inverted flag (2.3ms)[0m
  * test grepping multiples files at once one match, match entire lines flag[32m  * test grepping multiples files at once one match, match entire lines flag (1.7ms)[0m
  * test grepping multiples files at once one match, multiple flags[32m  * test grepping multiples files at once one match, multiple flags (2.1ms)[0m
  * test grepping multiples files at once no matches, various flags  * test grepping multiples files at once no matches, various flags (skipped)


Finished in 0.3 seconds (0.2s on load, 0.1s on tests)
[32m21 tests, 0 failures, 1 skipped[0m

Randomized with seed 0
[33mwarning: [0mvariable file is unused
  grep.exs:16

Excluding tags: [:pending]


GrepTest
  * test grepping a single file one match, no flags[32m  * test grepping a single file one match, no flags (3.3ms)[0m
  * test grepping a single file one match, print line numbers flag[32m  * test grepping a single file one match, print line numbers flag (1.6ms)[0m
  * test grepping a single file one match, case-insensitive flag[32m  * test grepping a single file one match, case-insensitive flag (2.5ms)[0m
  * test grepping a single file one match, print file names flag[32m  * test grepping a single file one match, print file names flag (2.1ms)[0m
  * test grepping a single file one match, match entire lines flag[32m  * test grepping a single file one match, match entire lines flag (2.7ms)[0m
  * test grepping a single file one match, multiple flags[32m  * test grepping a single file one match, multiple flags (9.3ms)[0m
  * test grepping a single file several matches, no flags[32m  * test grepping a single file several matches, no flags (3.1ms)[0m
  * test grepping a single file several matches, print line numbers flag[32m  * test grepping a single file several matches, print line numbers flag (1.4ms)[0m
  * test grepping a single file several matches, match entire lines flag[32m  * test grepping a single file several matches, match entire lines flag (1.5ms)[0m
  * test grepping a single file several matches, case-insensitive flag[32m  * test grepping a single file several matches, case-insensitive flag (1.3ms)[0m
  * test grepping a single file several matches, inverted flag[32m  * test grepping a single file several matches, inverted flag (1.7ms)[0m
  * test grepping a single file no matches, various flags[32m  * test grepping a single file no matches, various flags (1.3ms)[0m
  * test grepping multiples files at once one match, no flags[32m  * test grepping multiples files at once one match, no flags (1.4ms)[0m
  * test grepping multiples files at once several matches, no flags[32m  * test grepping multiples files at once several matches, no flags (1.5ms)[0m
  * test grepping multiples files at once several matches, print line numbers flag[32m  * test grepping multiples files at once several matches, print line numbers flag (6.6ms)[0m
  * test grepping multiples files at once one match, print file names flag[32m  * test grepping multiples files at once one match, print file names flag (8.7ms)[0m
  * test grepping multiples files at once several matches, case-insensitive flag[32m  * test grepping multiples files at once several matches, case-insensitive flag (7.3ms)[0m
  * test grepping multiples files at once several matches, inverted flag[32m  * test grepping multiples files at once several matches, inverted flag (2.7ms)[0m
  * test grepping multiples files at once one match, match entire lines flag[32m  * test grepping multiples files at once one match, match entire lines flag (2.9ms)[0m
  * test grepping multiples files at once one match, multiple flags[32m  * test grepping multiples files at once one match, multiple flags (3.7ms)[0m
  * test grepping multiples files at once no matches, various flags[32m  * test grepping multiples files at once no matches, various flags (2.4ms)[0m


Finished in 0.2 seconds (0.2s on load, 0.09s on tests)
[32m21 tests, 0 failures[0m

Randomized with seed 0
